import { SearchDepartmentPipe } from './search-department.pipe';

describe('SearchDepartmentPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDepartmentPipe();
    expect(pipe).toBeTruthy();
  });
});
