import { SortDepartmentPipe } from './sort-department.pipe';

describe('SortDepartmentPipe', () => {
  it('create an instance', () => {
    const pipe = new SortDepartmentPipe();
    expect(pipe).toBeTruthy();
  });
});
